version: 2
distro_name: "custom-ollama"
apis:
  - inference

providers:
  inference:
    - provider_id: ollama
      provider_type: remote::ollama
      config:
        base_url: http://localhost:11434/v1

server:
  port: 8321
  workers: 1